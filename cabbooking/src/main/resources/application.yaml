server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cabbooking
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    user:
      name: admin
      password: admin

stripe:
  keys:
    secret: sk_test_dummyKey123